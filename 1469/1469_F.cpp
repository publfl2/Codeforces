#include <stdio.h>

int main()
{
	int a,b;
	scanf("%d%d",&a,&b);
	for(int i=1;i<=a;i++) scanf("%d",&x[i]);
	std::sort(x+1,x+a+1);
	
	int min = 1, max = 200000;
	while(min<=max)
	{
		int h = (min+max)/2;
		for(int i=1;i<=a;i++)
		{
			S += x[i];
		}
	}
}
