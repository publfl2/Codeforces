#include <stdio.h>

int main()
{
	int a,b,c;
	scanf("%d%d%d",&a,&b,&c);
	for(int i=1;i<=a;i++) scanf("%d",&x[i]);
	for(int i=2;i<=a;i++)
	{
		int s = func(x[1],x[i]);
		if(M[s]>0)
		{
			if()
		}
		M[s]++;
		
	}
	
}
