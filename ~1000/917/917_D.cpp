#include <stdio.h>
#include <vector>

int DFSNumber[110];
int backDFS[110];
void dfs(int node, int parent, int& number)
{
	
}
int main()
{
	int a;
	scanf("%d",&a);
	for(int i=1;i<a;i++)
	{
		scanf("%d%d",&c,&d);
		V[c].push_back(d);
		V[d].push_back(c);
	}
	dfs(1,0,1);
}
